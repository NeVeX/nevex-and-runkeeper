<cronentries>
    <cron>
        <url>/admin/schedule/job/comments</url>
        <description>Add comments to users activities</description>
        <schedule>every 5 minutes</schedule>
        <retry-parameters>
            <min-backoff-seconds>5</min-backoff-seconds>
            <max-doublings>3</max-doublings>
        </retry-parameters>
    </cron>
</cronentries>

